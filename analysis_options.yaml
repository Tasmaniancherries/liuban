include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - build/**
    - .dart_tool/**
    - ios/Pods/**
    - "**/Pods/**"
  language:
    strict-inference: true
    strict-casts: true

linter:
  rules:
    prefer_const_constructors: true
    prefer_const_literals_to_create_immutables: true
    # Catch fire-and-forget [Future]s that should use await, unawaited(), or unawaitedDebug*.
    unawaited_futures: true
    # Stricter: flag *statement-level* discarded async results (pairs with unawaited_futures).
    discarded_futures: true
    cancel_subscriptions: true
    close_sinks: true
    # void fn() async { ... } hides errors from callers; prefer Future<void>.
    avoid_void_async: true

    # Extra rigor beyond flutter_lints defaults (keep CI aligned with local).
    always_declare_return_types: true
    avoid_slow_async_io: true
    combinators_ordering: true
    directives_ordering: true
    prefer_final_locals: true
    prefer_interpolation_to_compose_strings: true
    prefer_single_quotes: true
    require_trailing_commas: true
    sized_box_for_whitespace: true
    unnecessary_brace_in_string_interps: true
    use_colored_box: true
    use_full_hex_values_for_flutter_colors: true
    use_is_even_rather_than_modulo: true
    use_string_buffers: true
    use_super_parameters: true
