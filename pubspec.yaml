name: liuban
description: 留伴 — 在港留學生社交（Flutter，iOS + Android）
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^14.6.2
  image_picker: ^1.1.2
  dio: ^5.9.0
  shared_preferences: ^2.5.3
  app_links: ^7.0.0
  share_plus: ^10.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  test: ^1.30.0

flutter:
  uses-material-design: true
